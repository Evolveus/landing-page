import Design5Hybrid from './designs/Design5Hybrid';

export default function LandingPage({ onBrochure, onThemes, onPresentation }) {
  return <Design5Hybrid onBrochure={onBrochure} onThemes={onThemes} onPresentation={onPresentation} />;
}
